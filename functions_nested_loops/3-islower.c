#include "main.h"
/**
 *_islower check lowercase characters
 * Write a function that checks for lowercase character.
 * Return: void
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
